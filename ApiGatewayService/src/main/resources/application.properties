# ----------------------------------------
# SERVER PROPERTIES
# ----------------------------------------
# EMBEDDED SERVER CONFIGURATION (ServerProperties)
# Network address to which the server should bind.
#server.address= 127.0.0.1
# Server HTTP port.
server.port=8080
# Value to use for the Server response header (if empty, no header is sent).
server.server-header= TESTSYSTEM


# ----------------------------------------
# Application Properties
# ----------------------------------------
spring.application.name=ApiGatewayService

# ----------------------------------------
# Logging Configuration
# ----------------------------------------
# set logging level for package edu.fra.uas
logging.level.edu.fra.uas=DEBUG
# set logging level for autoconfiguration output
# to disable conditions evaluation report
logging.level.org.springframework.boot.autoconfigure=ERROR

# Value to enable SWagger UI in Codespaces
server.forward-headers-strategy=framework
# ----------------------------------------
# Colorful log output
# ----------------------------------------
spring.output.ansi.enabled=ALWAYS

# ----------------------------------------
# Service URLs
# ----------------------------------------
# Expense Tracker Service URL
expenseServiceUrl=http://localhost:8081

# Currency Converter Service URL
converterServiceUrl=http://localhost:8082/api/converter/convert

# User Service URL
userServiceUrl=http://localhost:8083/users